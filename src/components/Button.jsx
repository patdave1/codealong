import React from "react";

const Button = ({lable}) => {
    return(
        <Button className="border-2 border-orange-500 py-2 px-5 rounded-full text-orange-500 hover: bg-white">{lable}</Button>
    )
}
export default Button